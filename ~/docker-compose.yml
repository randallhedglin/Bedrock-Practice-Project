version: '3.7'

services:
  db:
    container_name: locallens-wp-theme_mysql
    environment:
      MYSQL_ROOT_PASSWORD: somewordpress
      MYSQL_DATABASE: wordpress
      MYSQL_USER: wordpress
      MYSQL_PASSWORD: wordpress
    image: mariadb # necessary for M1 chips
    command: --max_allowed_packet=268435456
    ports:
      - 33060:3306
    restart: "no"
    volumes:
      - ./db_data:/var/lib/mysql:delegated

  nginx-proxy:
    container_name: locallens-wp-theme_nginx-proxy
    depends_on:
      - wordpress
    image: jwilder/nginx-proxy
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./certs:/etc/ssl/certs:ro
      - ./my_nginx.conf:/etc/nginx/nginx.conf:ro
      - /var/run/docker.sock:/tmp/docker.sock:delegated
  
  phpmyadmin:
    container_name: locallens-wp-theme_phpmyadmin
    environment:
      PMA_ARBITRARY: 1
      PMA_HOST: db
      PMA_USER: wordpress
      PMA_PASSWORD: wordpress
      PMA_PORT: 3306
      UPLOAD_LIMIT: 256M
    image: phpmyadmin
    links:
      - db
    ports:
      - 8080:80
    restart: always

  wordpress:
    container_name: locallens-wp-theme_wordpress
    depends_on:
      - db
    environment:
      WORDPRESS_DB_HOST: db:3306
      WORDPRESS_DB_USER: wordpress
      WORDPRESS_DB_PASSWORD: wordpress
      WORDPRESS_DB_NAME: wordpress
      VIRTUAL_HOST: locallens.local
    expose:
      - 9003
    image: wordpress_xdebug
    restart: "no"
    volumes:
      - ./:/var/www/html:delegated
      - ./uploads.ini:/usr/local/etc/php/conf.d/uploads.ini:ro
      - ./xdebug.ini:/usr/local/etc/php/conf.d/xdebug.ini:ro

volumes:
  db_data:
